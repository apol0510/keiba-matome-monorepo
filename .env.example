# ============================================
# monorepo統合環境変数設定
# ============================================
#
# このファイルを .env にコピーして使用してください
# cp .env.example .env
#

# ============================================
# 共通環境変数（全プロジェクト共通）
# ============================================

# Airtable API Key（全プロジェクト共通 - 3つのBaseすべてにアクセス可能）
# 取得方法: https://airtable.com/create/tokens
# 必要なScopes: data.records:read, data.records:write, schema.bases:read
# 必要なAccess: appdHJSC4F9pTIoDj, appt25zmKxQDiSCwh, appKPasSpjpTtabnv
export AIRTABLE_API_KEY="YOUR_AIRTABLE_TOKEN_HERE"

# Anthropic (Claude) API Key
# 取得方法: https://console.anthropic.com/ → API Keys
export ANTHROPIC_API_KEY="YOUR_ANTHROPIC_API_KEY_HERE"

# Discord Webhook URL（監視システム通知、オプション）
# 取得方法: Discord サーバー設定 → 連携サービス → ウェブフック
export DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/YOUR_WEBHOOK_URL"

# ============================================
# X (Twitter) API認証情報（JSON形式）
# ============================================
#
# 全サイトの認証情報を1つのJSON文字列にまとめます
# 各サイトごとに異なるアカウントを使用できます
#
# 取得方法: https://developer.twitter.com/ → Projects & Apps
#
# JSON形式（改行なし）:
export X_CREDENTIALS_JSON='{
  "KEIBA_MATOME_X": {
    "apiKey": "YOUR_KEIBA_MATOME_API_KEY",
    "apiSecret": "YOUR_KEIBA_MATOME_API_SECRET",
    "accessToken": "YOUR_KEIBA_MATOME_ACCESS_TOKEN",
    "accessSecret": "YOUR_KEIBA_MATOME_ACCESS_SECRET"
  },
  "CHIHOU_X": {
    "apiKey": "YOUR_CHIHOU_API_KEY",
    "apiSecret": "YOUR_CHIHOU_API_SECRET",
    "accessToken": "YOUR_CHIHOU_ACCESS_TOKEN",
    "accessSecret": "YOUR_CHIHOU_ACCESS_SECRET"
  },
  "YOSOU_X": {
    "apiKey": "YOUR_YOSOU_API_KEY",
    "apiSecret": "YOUR_YOSOU_API_SECRET",
    "accessToken": "YOUR_YOSOU_ACCESS_TOKEN",
    "accessSecret": "YOUR_YOSOU_ACCESS_SECRET"
  }
}'

# ============================================
# Netlify Build Hooks（サイトごと）
# ============================================
#
# 取得方法: Netlify Site Settings → Build & deploy → Build hooks
#
export KEIBA_MATOME_NETLIFY_BUILD_HOOK="https://api.netlify.com/build_hooks/YOUR_KEIBA_MATOME_HOOK"
export CHIHOU_KEIBA_NETLIFY_BUILD_HOOK="https://api.netlify.com/build_hooks/YOUR_CHIHOU_HOOK"
export YOSOU_KEIBA_NETLIFY_BUILD_HOOK="https://api.netlify.com/build_hooks/YOUR_YOSOU_HOOK"

# ============================================
# プロジェクト固有のBase ID（sites-config.jsonで管理）
# ============================================
#
# これらは .env には不要です（設定ファイルで管理）
# 参考情報として記載：
#
# keiba-matome: appdHJSC4F9pTIoDj
# chihou-keiba-matome: appt25zmKxQDiSCwh
# yosou-keiba-matome: appKPasSpjpTtabnv
