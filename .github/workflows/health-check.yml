name: monorepo統合監視（毎日9AM JST）

on:
  schedule:
    # 毎日9:00 JST（0:00 UTC）
    - cron: '0 0 * * *'
  workflow_dispatch: # 手動実行も可能

jobs:
  health-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Node.js 20セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 依存関係をインストール
        run: |
          cd packages/shared
          npm install

      - name: 全サイトの健全性チェック
        env:
          AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          node packages/shared/scripts/monitor-all-sites.cjs

      - name: 結果をDiscord通知（ワークフロー失敗時）
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -H "Content-Type: application/json" \
            -d "{\"content\":\"🚨 **monorepo統合監視が失敗しました**\n\nワークフロー: health-check\n実行時刻: $(date -u '+%Y-%m-%d %H:%M:%S UTC')\n\n[ログを確認](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})\"}" \
            $DISCORD_WEBHOOK_URL
